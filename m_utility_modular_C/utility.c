#include <stdio.h>
#include "utility.h"

void show_message() {
    printf("Utility Program\n");
}

long sqr(int x) {
    return x * x;
}

int double_number(int x) {
    return x * 2;
}

int HALF(int x) {
    return x / 2;
}